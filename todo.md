# Task: Add /workbench Command with /craft Alias

## Objective
Add a new command `/workbench` (and alias `/craft`) which opens a crafting table interface.

## Steps to Complete:
- [ ] 1. Add command registration for `/workbench` and `/craft` in EasyCommandsRecode.java
- [ ] 2. Implement server-side logic to open crafting table interface
- [ ] 3. Add language entries for command messages in en_us.json
- [ ] 4. Test the implementation
- [ ] 5. Verify the commands work correctly

## Implementation Details:
- Permission level: 2 (standard for quality-of-life commands)
- Commands should only work for players (not console/command blocks)
- Use direct client GUI interaction approach
- Follow existing patterns in the codebase
